#include <stdio.h>

int main ()
{

    int a;
    int b; 
    
    printf("inserisci i due numeri \n");
    scanf ("%d %d", &a, &b);
    int divisione = a / b;
    printf("il risultato della divisione è: %d\n", divisione);

}

